import React from 'react'
import LocalMallIcon from '@mui/icons-material/LocalMall';

export const Cart = () => {
  return (
    <LocalMallIcon fontSize="large" sx={{ color: "black "}} data-testid="LocalMallIcon"/>
  )
}
